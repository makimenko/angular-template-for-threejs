import { CylindermeshDirective } from './cylindermesh.directive';

describe('CylindermeshDirective', () => {
  it('should create an instance', () => {
    const directive = new CylindermeshDirective();
    expect(directive).toBeTruthy();
  });
});
