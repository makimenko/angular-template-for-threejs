import { SpheremeshDirective } from './spheremesh.directive';

describe('SpheremeshDirective', () => {
  it('should create an instance', () => {
    const directive = new SpheremeshDirective();
    expect(directive).toBeTruthy();
  });
});
